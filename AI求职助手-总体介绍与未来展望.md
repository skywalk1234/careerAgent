# 微光职引 · AI 求职助手总体介绍与未来展望

> 定位：产品向的总览文档，不涉及具体代码改动
> 状态：功能部分反映 2026-09-02 已实现现状；展望部分为规划设想
> 平台代号：career_agent

## 一、一句话介绍

微光职引是一套**长期陪伴式求职智能体**：它以用户的简历与真实岗位库为事实底座，由一个主对话 LLM 统一调度多个**专家子代理**，围绕「求职→积累→成长」为学生提供 **AI 求职问答、RAG 岗位推荐、简历分析与润色**等服务，并规划引入**长期记忆、职业规划专家、模拟面试**，让模型对用户的理解持续累积，陪用户走完整条求职与职业成长之路。

## 二、产品定位与设计理念

### 2.1 为什么是「Agent」，而不是普通的 AI 问答

- **实时真实数据**：简历、岗位详情实时拉取，回答都锚定在用户的真实状态上，而非大模型的静态知识。
- **拒绝幻觉**：简历重写等高风险动作遵守「只重组用户已有事实，绝不虚构」红线。
- **主 LLM 统一编排**：一个主对话 LLM 理解意图、调度工具、维护多轮对话，让服务端零状态、不设流程状态机，新专家只需注册成工具即可平滑接入。

### 2.2 用户旅程（当前已实现的闭环）

```
用户简历 → AI 求职助手（主 LLM）
              ├─ 问答 / 查岗位详情（query_job_detail）──────── 实时信息
              ├─ 岗位推荐（recommend_specific_jobs）────────── RAG 精排
              ├─ 简历分析（analyze_resume）─────────────────── 专家诊断
              └─ 简历润色（polish_resume）────────────────── 专家改写+另存新简历
                 （每步都可让用户插话补充信息，多轮澄清由主 LLM 记忆并汇总）
```

# 

## 四、当前已实现功能

### 4.1 主对话 AI 求职助手

一个主对话 LLM 以 SSE 流式回复，串起所有工具与多轮对话。它能做的，按用户的一句话划分：

| 用户说                  | 系统识别 & 执行                                                                 | 核心工具                    |
| -------------------- | ------------------------------------------------------------------------- | ----------------------- |
| 我简历怎么样 / 帮我看看简历      | 拉取用户简历，RAG 召回相似简历的历史点评参考，专业诊断（语言表达、结构条理、内容完整性、可选的 JD 契合度），诊断后可能附带 1~3 个追问 | analyze_resume          |
| 针对 [某岗位] 帮我优化/润色简历   | 拉简历 + 目标岗位 JD，按岗位对齐改写措辞与强调重点，**自动另存为新简历**（原简历保留），对话里只回变更摘要                | polish_resume           |
| 我的目标岗位是什么 / 我该投什么岗   | 从对话抽取用户求职画像，RAG 检索真实岗位库 + DeepSeek 精排，返回最佳匹配 + 备选岗位及匹配分                   | recommend_specific_jobs |
| 这个岗位 [jobId] 是什么/怎么样 | 实时查岗位详情（工作内容、能力要求、薪资）                                                     | query_job_detail        |
| （通用提问/闲聊）            | 兜底实时查用户简历画像回答                                                             | get_student_profile     |

### 4.2 简历分析与润色（专家子代理）

**为什么简历是这套系统的核心资产**：简历由「结构化字段」升级为「markdown 原文」存储，正好是分析与润色最理想的输入——完整的文字才能被专家读懂、改写。

**两位专家协作**

|      | 简历分析专家 analyze_resume         | 简历润色专家 polish_resume                    |
| ---- | ----------------------------- | --------------------------------------- |
| 用户意图 | 看 / 分析 / 评估 / 诊断简历            | 修改 / 润色 / 优化 / 改写简历（可针对岗位）              |
| 输入   | 简历 markdown（可选 JD + 可选历史点评参考） | 简历 markdown（可选 JD）+ 用户补充的修改要求           |
| 动作   | 只分析，绝不改简历                     | 重写整份简历，直接**另存为新简历**                     |
| 输出   | 文字诊断（≤300 字）+ 可选 1~3 个追问      | 只回变更摘要，不回传完整 markdown                   |
| 红线   | 诊断只基于简历实际内容，不虚构经历             | 只重组/改写/扩写用户已给内容，严禁编造公司/项目/数字；reflect 自检 |

**关键设计：一次完整的「分析 → 润色」是一段自然对话**

```
用户：帮我看看简历 → 分析专家出诊断，顺带追 1~3 个问题
用户：回答 / 说「其实我想投 xx，之前实习做过接口压测优化，帮我针对这个岗位优化」[jobId:42]
→ 主 LLM 把「岗位目标 + 用户补充」汇总进 extra_info，调润色专家
→ 润色专家对齐 JD 改写 → 另存新简历（原简历保留）→ 对话回「变更摘要 + 已另存新简历」
```

**安全设计（防幻觉，防误伤）**

1. 润色永远另存新简历，原简历不覆盖——即使改错了，原稿还在；
2. 润色 prompt 内置「不虚构」红线 + reflect 自检；
3. 对话只回变更摘要（不回传完整 markdown），规避超长简历被流式输出截断；
4. 分析诊断限 300 字，只基于简历实际内容，不虚构经历。

### 4.3 岗位推荐：从简历 / 求职意愿到具体岗位（人岗匹配）

用户求职意图 → 推荐**具体的可投岗位**，主流程：

**主流程（具体岗位推荐 recommend_specific_jobs）**

1. 抽取用户画像：目标岗位、目标城市、薪资期望、技能栈、经验水平（主 LLM 从对话抽取，全自动填参）；
2. 将画像拼成查询文本，用 text-embedding-v1 编码成 1536 维向量；
3. 在**真实岗位向量库**（8.147.71.59:40086，`job_detail_vector` 表）做余弦相似度检索，召回 Top-K 候选岗位；
4. 把候选岗位 + 用户画像喂给 DeepSeek 做**精排**（重排序），产出：
   - **1 个最佳匹配岗位**：带综合匹配分 overallScore（0-100）+ 四维评分（基础要求 / 专业技能 / 职业素养 / 发展潜力）+ 匹配标签（如「高匹配」「薪资略低但发展好」「技术栈重合」）；
   - **最多 4 个备选岗位**：带综合分 + 匹配标签。

**同链路还提供两条辅助入口**（相同底层向量检索）

- 大类推荐（`job_category_vector`）：先命中岗位大类，作为导航/兜底；
- 该能力也被 **career-service 通过 Feign 直连**复用（`/jobs/recommend*`），服务间调用走同一套逻辑。

**典型一句话用法**
「我是 Java 方向，想去上海，期望 15-25k，会 Spring/Redis，帮我看看有什么合适的岗位」→ 系统返回最佳匹配 + 备选，带匹配分与标签。

### 4.4 底层能力（对用户透明，对工程是骨架）

| 能力                  | 作用                                                                                                           |
| ------------------- | ------------------------------------------------------------------------------------------------------------ |
| pgvector 语义检索       | 简历/岗位语义相似度检索（岗位真实库，1536 维）                                                                                   |
| 简历样例库（进行中）          | 沉淀「历史他人简历 + 专家点评」成对样例，分析/润色时召回作 few-shot 参考，让专家站在同行评判标准上诊断。**现状**：建表完成、检索函数与 analyze 接入已实现，等待人工整理语料入库（P2）后生效 |
| MySQL（chat_history） | 保存每轮会话与消息，支撑多轮记忆                                                                                             |
| RabbitMQ            | 简历写回等异步任务落库                                                                                                  |

## 五、未来展望（规划路线）

### 5.1 近期 / 已铺设好地基的方向

- **打通「分析 → 润色 → 重新分析」的闭环**：分析、润色接口已就绪，梳理好 UI 与话术即可端到端跑通。
- **岗位推荐 → 简历优化**：把某岗位的 bestMatch 的 JD 作为润色方向，用户可一键把简历对齐到推荐岗位。
- **推荐再扩展**：支持批量多个岗位、加入已投递/收藏状态、主动在求职旺季推送。

### 5.2 长期记忆：让模型记住用户是谁（画像层）

**痛点**：当前对话能力随会话结束而消失，模型每次都是「从零认识你」。要记住的东西恰恰藏在对话里：学到哪了、新学了什么技术、项目有什么进展、在追什么岗位。

**引入长期记忆机制**：把对话中的关键事实抽取成持久化的用户画像字段，未来每次对话前加载，让助手始终带着对用户的完整认识回应用户。

> 为什么它重要：**长期记忆是「职业规划专家」和「模拟面试官」这两个未来的专家的地基**——它们必须了解用户当前的学习进度与积累，否则产出的规划与提问都会流于泛化。

### 5.3 职业规划专家：给出努力方向与规划方案

由专门的 LLM 负责，结合**长期记忆中的用户画像**与行业/岗位信息，为用户生成**未来一段时间的努力方向与规划方案**——比单次推荐更长期、更系统，沉淀成用户持续积累的依据。

### 5.4 模拟面试专家：把简历变成实战演练

由单独 LLM 负责，接收**用户简历 + 意向岗位**，模拟面试官对用户提问（技术面/行为面），结束后生成**面试报告**（薄弱项、待补强点）。让学生在校招前提前体验真实面试节奏。

### 5.5 未来的完整蓝图：从「单次求职工具」到「长期职业陪伴」

```
【现在：工具化】                【未来：陪伴化】
┌──────────────────────┐     ┌────────────────────────────┐
│  AI 求职助手         │     │  AI 求职助手（总入口）      │
│  · 实时简历画像      │     │    │ 对话 + 全局记忆         │
│  · 问答/查岗位       │     │    ├── 简历分析专家          │
│  · 具体岗位推荐      │     │    ├── 简历润色专家          │
│  · 简历分析          │     │    ├── 岗位推荐（RAG 精排）  │
│  · 简历润色          │     │    └── 职业规划专家（未来）  │
│                      │     │    └── 模拟面试专家（未来）  │
│  记忆: 无(单会话)    │     │  记忆: 长期画像 + 学习进展    │
└──────────────────────┘     └────────────────────────────┘
「用完即走」                「陪你成长」：你学到哪，它就跟到哪
```

> 展望假设（仅供参考）：新增的长期记忆 / 未来规划专家 / 模拟面试专家，均以**独立 LLM + 主 LLM 调度**的方式追加，不推倒现有架构。具体落地仍取决于长期记忆如何统一抽取（未实现、需讨论）。

## 六、总结

微光职引已经做到了「AI 真的能基于我的真实简历和真实岗位库，帮我分析、帮我改、帮我找岗位」。接下来要做的是**让它「记得住、陪得久」**：引入长期记忆让模型把对话沉淀成对用户的总体画像，补充职业规划专家给出成长方向，补充模拟面试专家把求职变成实战演练——从一次性问答工具，走向真正长期陪伴的求职与职业成长助手。
