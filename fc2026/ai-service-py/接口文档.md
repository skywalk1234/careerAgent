# AI 求职助手服务 · 接口文档

基于 **DeepSeek** 的求职助手聊天服务（FastAPI），端口 **8086**，不经过 Java 网关，前端直连。

## 快速开始

| 入口                  | 地址                           |
| ------------------- | ---------------------------- |
| 交互式测试页面（Swagger UI） | http://localhost:8086/docs   |
| 接口文档（ReDoc）         | http://localhost:8086/redoc  |
| 健康检查                | http://localhost:8086/health |

启动命令（在 `ai-service-py` 目录下）：

```powershell
.\venv\Scripts\python.exe main.py
```

> 注意：Windows 下已关闭 uvicorn 的 reload 模式，改代码后需手动重启。

## 通用约定

- **Base URL**：`http://localhost:8086`
- **数据格式**：JSON（`Content-Type: application/json`）
- **鉴权**：无（请求直连 AI 服务，不经过网关，无需 JWT）
- **统一前缀**：聊天接口都在 `/users/me/home/assistant` 下；岗位推荐接口在 `/jobs/recommend*` 下
- **字段风格**：响应字段使用**驼峰**（`sessionId` / `messageId` / `createdAt`），与前端约定一致
- **时间格式**：ISO 8601 字符串（UTC）
- **默认用户**：直连阶段不传用户标识，服务统一使用默认用户 `"111"`

### 统一响应外壳

聊天接口（除 SSE 流和 `/health`）都包一层响应壳，与前端 `services/http.ts` 约定一致。**岗位推荐接口（`/jobs/recommend*`）例外**：返回 `text/plain` 裸 JSON 文本、不套响应壳（见第 7、8 节），调用方直接解析 JSON 内容：

```json
{
  "code": 200,
  "msg": "success",
  "data": { }
}
```

**错误响应**同样使用该结构（HTTP 状态码与 `code` 一致）：

```json
{
  "code": 404,
  "msg": "会话不存在",
  "data": null
}
```

| HTTP 状态码 | code | 场景                               |
| -------- | ---- | -------------------------------- |
| 200      | 200  | 成功                               |
| 404      | 404  | 会话或消息不存在                         |
| 422      | 422  | 请求参数校验失败                         |
| 500      | 500  | 未配置 `DEEPSEEK_API_KEY` 或 AI 调用出错 |

---

## 1. 健康检查

### `GET /health`

无需参数，用于确认服务存活。

**响应示例：**

```json
{
  "status": "ok"
}
```

---

## 2. 创建会话

### `POST /users/me/home/assistant/sessions`

创建一条新的对话会话（标题默认 `"新对话"`）。

**请求体（对齐前端 `createHomeSession`）：**

| 字段            | 类型     | 必填  | 说明               |
| ------------- | ------ | --- | ---------------- |
| `scene`       | string | 否   | 场景标识，默认 `"home"` |
| `temperature` | number | 否   | 采样温度，默认 `0.3`    |

```json
{
  "scene": "home",
  "temperature": 0.3
}
```

**响应 `200`：**

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "sessionId": "0556f155b12047758ac48f3a2d6e8819",
    "createdAt": "2026-08-02T11:31:58.983442",
    "welcomeMessage": null
  }
}
```

---

## 3. 获取会话列表

### `GET /users/me/home/assistant/sessions`

获取当前用户的会话列表（按「置顶 + 最近更新」排序），无请求参数。

**响应 `200`（`data.list` 项对齐前端 `HomeSession`）：**

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "total": 2,
    "list": [
      {
        "sessionId": "0556f155b12047758ac48f3a2d6e8819",
        "title": "新对话",
        "lastMessagePreview": "前端开发方向建议从 HTML/CSS 基础开始…",
        "pinned": false,
        "favorited": false,
        "updatedAt": "2026-08-02T11:31:58.983442"
      }
    ]
  }
}
```

---

## 4. 获取会话消息列表

### `GET /users/me/home/assistant/sessions/{sessionId}/messages`

获取某个会话的全部消息（按时间正序）。

**路径参数：**

| 字段          | 类型     | 说明    |
| ----------- | ------ | ----- |
| `sessionId` | string | 会话 ID |

**响应 `200`（`data.list` 项对齐前端 `HomeMessage`）：**

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "sessionId": "0556f155b12047758ac48f3a2d6e8819",
    "total": 1,
    "list": [
      {
        "messageId": "448d7e72221b45f9baf4fb34ca38cb6b",
        "role": "user",
        "content": "我是一名前端开发，想转 AI 方向",
        "status": "succeeded",
        "createdAt": "2026-08-02T11:31:59.111408",
        "fileNames": [],
        "agentTrace": {},
        "actions": [],
        "taskResultCard": null
      },
      {
        "messageId": "e88c8abedcac4f93adff2349bc366484",
        "role": "assistant",
        "content": "从前端转 AI 方向，建议从 Python 和机器学习基础开始…",
        "status": "succeeded",
        "createdAt": "2026-08-02T11:31:59.117395",
        "fileNames": [],
        "agentTrace": {
          "steps": [],
          "status": "succeeded",
          "activeStepId": null
        },
        "actions": [],
        "taskResultCard": null
      }
    ]
  }
}
```

---

## 5. 发送消息

### `POST /users/me/home/assistant/sessions/{sessionId}/messages`

把用户消息存入数据库，并返回 **SSE 流地址**，前端随后连接该地址接收 AI 增量回复（两步式设计，避免长等待阻塞）。

**路径参数：**

| 字段          | 类型     | 说明    |
| ----------- | ------ | ----- |
| `sessionId` | string | 会话 ID |

**请求体（对齐前端 `createHomeSessionMessage`）：**

| 字段              | 类型     | 必填  | 说明                                     |
| --------------- | ------ | --- | -------------------------------------- |
| `content`       | string | 是   | 用户输入的消息内容（至少 1 个字符）                    |
| `temperature`   | number | 否   | 采样温度，默认 `0.4`                          |
| `traceId`       | string | 否   | 链路追踪 ID                                |
| `fileContext`   | object | 否   | `{hasResumeFile, fileNames}`           |
| `pageContext`   | object | 否   | 页面上下文                                  |
| `executionMode` | string | 否   | `"auto"` / `"chat"` / `"task"`         |
| `goal`          | string | 否   | 任务目标                                   |
| `taskPolicy`    | object | 否   | `{allowToolCall, maxSteps, timeoutMs}` |
| `toolContext`   | object | 否   | 工具上下文                                  |

```json
{
  "content": "帮我分析前端转 AI 岗位的可行性",
  "temperature": 0.4,
  "traceId": "trace_home_1722...",
  "fileContext": { "hasResumeFile": false, "fileNames": [] },
  "pageContext": null,
  "executionMode": "auto",
  "goal": "",
  "taskPolicy": { "allowToolCall": true, "maxSteps": 8, "timeoutMs": 120000 },
  "toolContext": null
}
```

> 说明：当前版本只使用 `content`，其余字段仅做兼容接收（后续接入 Agent 任务时启用）。

**响应 `200`（对齐前端 `createHomeSessionMessage`）：**

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "sessionId": "0556f155b12047758ac48f3a2d6e8819",
    "userMessage": {
      "messageId": "448d7e72221b45f9baf4fb34ca38cb6b",
      "role": "user",
      "content": "帮我分析前端转 AI 岗位的可行性",
      "status": "succeeded",
      "createdAt": "2026-08-02T11:31:59.111408",
      "fileNames": [],
      "agentTrace": {},
      "actions": [],
      "taskResultCard": null
    },
    "assistantMessage": {
      "messageId": "e88c8abedcac4f93adff2349bc366484",
      "role": "assistant",
      "content": "",
      "status": "processing",
      "createdAt": "2026-08-02T11:31:59.117395",
      "fileNames": [],
      "agentTrace": null,
      "actions": [],
      "taskResultCard": null
    },
    "stream": {
      "protocol": "sse",
      "url": "/users/me/home/assistant/sessions/0556f155b12047758ac48f3a2d6e8819/messages/448d7e72221b45f9baf4fb34ca38cb6b/stream"
    }
  }
}
```

> `assistantMessage` 是占位对象（内容为空、状态 `processing`），前端拿它的 `messageId` 去连接 SSE 流，真实内容由流式事件推送。
> 
> 用户发送消息之后，先占住一个回复的位置

---

## 6. 流式接收 AI 回复（SSE）

### `GET /users/me/home/assistant/sessions/{sessionId}/messages/{messageId}/stream`

以 **Server-Sent Events（SSE）** 协议逐字推送 AI 生成内容，前端通过 `EventSource` 或 fetch 流式读取。回复生成完毕后自动保存助手消息到数据库。

**路径参数：**

| 字段          | 类型     | 说明                               |
| ----------- | ------ | -------------------------------- |
| `sessionId` | string | 会话 ID                            |
| `messageId` | string | 第 5 步返回的 `userMessage.messageId` |

**注意：** 未配置 `DEEPSEEK_API_KEY` 时返回 `500`，需在 `.env` 中填写。

### SSE 事件协议

每条 SSE 消息格式：`event: <事件名>\ndata: <JSON>\n\n`

| 事件名     | 触发时机    | data 结构                                              |
| ------- | ------- | ---------------------------------------------------- |
| `start` | 开始生成    | `{"type":"start","messageId":"..."}`                 |
| `delta` | 每生成一段文字 | `{"type":"delta","delta":"增量文本","content":"累计完整文本"}` |
| `done`  | 生成完成    | `{"type":"done","message":{...助手消息完整对象...}}`         |
| `error` | 处理出错    | `{"type":"error","messageId":"...","error":"错误信息"}`  |

**完整事件流示例：**

```tex
event: start
data: {"type":"start","messageId":"448d7e72..."}

event: delta
data: {"type":"delta","delta":"前端","content":"前端"}

event: delta
data: {"type":"delta","delta":"转AI","content":"前端转AI"}

# 完整的一个trace
event: trace
data: {"type": "trace", "trace": {"traceVersion": "1.0", "mode": "chat", "status": "processing", "startedAt": "2026-08-03T10:00:00+00:00", "finishedAt": null, "activeStepId": null, "steps": [{"stepId": "step_1", "type": "thought", "title": "正在理解你的请求", "status": "succeeded"}]}}

#涉及到工具调用
event: trace
data: {"type": "trace", "trace": {"traceVersion": "1.0", "mode": "chat", "status": "processing", "startedAt": "2026-08-03T10:00:00+00:00", "finishedAt": null, "activeStepId": "step_2", "steps": [{"stepId": "step_1", "type": "thought", "title": "正在理解你的请求", "status": "succeeded"}, {"stepId": "step_2", "type": "tool", "title": "调用工具：recommend_specific_jobs", "status": "processing", "toolName": "recommend_specific_jobs", "toolParams": {"job_intention": "Java后端", "city": "杭州"}}]}}


event: done
data: {"type":"done","message":{"messageId":"e88c8abe...","role":"assistant","content":"前端转AI…","status":"succeeded","actions":[],"agentTrace":{"status":"succeeded","activeStepId":null}}}
```

**前端消费伪代码：**

```js
const es = new EventSource(url)
es.addEventListener('delta', (e) => {
  const data = JSON.parse(e.data)
  console.log(data.delta)   // 增量文本
})
es.addEventListener('done', (e) => {
  es.close()                // 完成后关闭连接
})
es.addEventListener('error', (e) => {
  console.error(e)
})
```

---

## 7. 岗位大类推荐

### `POST /jobs/recommend`

根据用户画像 + 求职意愿做向量检索，推荐适合的**岗位大类**（无 LLM 参与）。供 Java `career-service` Feign 直连（服务间调用），前端主流程不直接调用。

**请求体：** 普通 JSON 对象即可。Python 会对请求体**整段文本**做向量嵌入后再余弦检索，因此建议把画像与求职意愿都放进去；也兼容 Feign 的「JSON 字符串字面量」双编码格式（`decode_query_text` 自动解码一层后按内层文本嵌入）。

```json
{
  "userId": "111",
  "求职意愿": {
    "preferredJobKeywords": ["前端开发"],
    "cityIntents": ["杭州", "西安"]
  },
  "profile": { "姓名": "张三", "技能": ["Java", "Spring Boot"] }
}
```

**处理流程：**

1. 对请求体文本嵌入（DashScope `text-embedding-v1`，1536 维）
2. 在 `job_category_vector` 表余弦检索：`topK=5`、相似度阈值 `0.3`
3. 返回每条结果的 `metadata.nodeName` 组成的数组

**响应 `200`（`text/plain`，裸 JSON 数组）：**

```json
["Java_国际化或特定领域技术工程师", "Python_数据分析工程师"]
```

无匹配结果或内部异常时返回空数组：

```json
[]
```

---

## 8. 具体岗位推荐（向量检索 + DeepSeek 排序）

### `POST /jobs/recommend/specific`

根据用户画像 + 求职意愿做向量检索，再由 DeepSeek 对全部候选岗位排序，第 1 名进 `bestMatch`、其余进 `otherRecommendations`，返回 **MatchJob 结构**。**前端「推荐岗位」功能直连此接口**（`aiHttp`，不经过 Java 网关）。

> **模型只负责排序**：输入每个候选的 `index / jobName / companyName / education / salaryText / content(JD 正文)`，输出 `[{"index": 2, "reason": "..."}, ...]`；岗位详情由 Python 用 `index` 回查向量库结果组装。
> 因此 Python **不再输出 `overallScore` / `matchTags` / `dimensionScores`**，改为每条一个 `reason`（排序理由）。

**请求体：** 同第 7 节，普通 JSON 对象（整段文本用于嵌入和 LLM 提示），建议包含 `求职意愿` + `profile`：

```json
{
  "userId": "111",
  "求职意愿": {
    "preferredJobKeywords": ["前端开发"],
    "cityIntents": ["杭州", "西安"],
    "salaryRange": "8k-16k",
    "benefits": ["双休", "五险一金"]
  },
  "profile": { "技能": ["Vue", "TypeScript"], "工作年限": "3年" }
}
```

**处理流程：**

1. 嵌入请求体 → 在 `job_detail_vector` 表余弦检索：`topK=5`、相似度阈值 `0.2`
2. 从每条结果的 metadata 提取候选信息（`jobName / companyName / education / salaryText`）连同该行 `content` 列的 JD 正文（超 4000 字符截断），编号 `index` 从 1 开始
3. DeepSeek（`deepseek-v4-flash`，关闭思考模式）对全部候选排序，只输出 `[{"index": n, "reason": "排序理由"}]`
4. 用 `index` 回查向量库结果，组装岗位详情（`jobId` 取 `metadata.jobKey`，与 PG `job_key` 列同值，前端据此调 `GET /jobs/{jobId}` 才能命中详情），第 1 名 → `bestMatch`，其余 → `otherRecommendations`，**全部候选都返回**
5. 模型侧任何异常（超时 / 非法 JSON / index 越界）都降级为**按向量相似度顺序**返回，`reason` 用兜底文案，请求不失败

**响应 `200`（`text/plain`，裸 JSON）：**

```json
{
  "bestMatch": {
    "jobId": "9c1f0a3b5d7e9f1a2b3c4d5e6f7a8b9c",
    "jobName": "Java 开发工程师",
    "companyName": "某科技公司",
    "city": "杭州",
    "educationRequirement": "本科",
    "salaryNegotiable": false,
    "salaryNormalized": "15-25K·14薪",
    "updatedAtRaw": "2026-09-09",
    "level": "3-5年",
    "reason": "技术栈与你的 Java/Spring 背景高度重合，杭州符合城市意愿，薪资落在期望区间内"
  },
  "otherRecommendations": [
    {
      "jobId": "3f7c2d1e8a4b6c9d0e1f2a3b4c5d6e7f",
      "jobName": "Python 后端工程师",
      "companyName": "某数据公司",
      "city": "西安",
      "educationRequirement": "本科",
      "salaryNegotiable": true,
      "salaryNormalized": "面议",
      "updatedAtRaw": "2026-09-08",
      "level": "1-3年",
      "reason": "技能方向相关但主力语言是 Python，且薪资未标明，只能排在第二"
    }
  ]
}
```

> 字段说明：
>
> - `jobId` 取 `metadata.jobKey`，**与 PG `job_key` 列同值**（表上 UNIQUE、必然非空）；`metadata.jobId`（`boss:{encryptJobId}`）只是 BOSS 招聘帖 id，不要拿它当对外 jobId，否则前端 `GET /jobs/{jobId}` 查不到详情。
> - 每条都有 `reason`（AI 排序理由）；模型调用失败或返回非法时降级为兜底文案「与你的技能栈语义相近」。
> - `educationRequirement` / `level` / `salaryNormalized` 分别取自 metadata 的 `education` / `experience` / `salaryText`；`salaryNormalized` 为空时下发 `null`，`salaryNegotiable` 为 `true`。
> - 不输出 `district` / `industryTags` / 各类分数：Java 侧 `ProcessMatchFilterService` 用裸 `ObjectMapper` 反序列化，多一个 DTO 未声明的键会抛 `UnrecognizedPropertyException`。

**错误响应**（HTTP 200，body 为 `text/plain` 的 error JSON，前端据 `error` 字段提示）：

```json
{ "error": "未找到匹配的岗位信息" }
```

| error 内容       | 场景                                 |
| -------------- | ---------------------------------- |
| `向量库不可用`       | pgvector 连接池初始化失败（服务仍可启动，只是无检索能力）  |
| `未找到匹配的岗位信息`   | 向量检索无结果（相似度都低于阈值 0.2）              |
| `系统内部错误: <异常>` | 其它运行时异常                            |

> DeepSeek 超时、返回非法 JSON、index 越界等都**不再返回 error**：降级为按向量相似度顺序返回全部候选，
> 每条 `reason` 用兜底文案，接口仍然 200。
> 注：全局助手工具 `recommend_specific_jobs` 另有一条 `岗位推荐服务暂不可用（向量库未连接）`。

---

## 附：完整调用时序（一次对话）

```
1. POST /sessions/{sessionId}           → 创建会话，拿到 sessionId
2. POST /sessions/{sessionId}/messages  → 存用户消息，拿到 assistantMessage.messageId + stream.url
3. GET  stream.url (SSE)                → 收 start/delta/done，AI 回复逐字展示
4. GET  /sessions/{sessionId}/messages  → 下次打开会话时展示历史
```

## 常见错误

| HTTP 状态码 | code | 场景                               |
| -------- | ---- | -------------------------------- |
| `404`    | 404  | 会话不存在 / 消息不存在                    |
| `422`    | 422  | 请求参数校验失败（如 `content` 为空）         |
| `500`    | 500  | 未配置 `DEEPSEEK_API_KEY` 或 AI 调用出错 |
