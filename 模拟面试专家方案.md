# 模拟面试专家（Mock Interview）方案 v0.1

> 状态：**设计稿（v0.1，待评审）**
> 日期：2026-09-05
> 落点（建议）：
> 
> - **对话/流程**：`fc2026/ai-service-py`（新 router `app/routers/interview.py` + 新 service `app/services/interview.py`），**仿照 [chat.py](ai-service-py/app/routers/chat.py) 的主 LLM 工具循环 + SSE 协议**；
> - **报告存储 / 查询**：**career-service（Java）** 新表 `user_interview_reports` 落 `careers` 库（网关 `career-user` 已覆盖 `/users/me/**`，无需新增路由）；
> - **面试过程数据**：Python 侧 MySQL `chat_history` 库新增 `interview_sessions` / `interview_messages` 两张表（面试问答逐条落库，供报告生成读取）。
>   前置结论：本文是对 [AI求职助手-总体介绍与未来展望.md](AI求职助手-总体介绍与未来展望.md) §5.4「模拟面试专家」与 [长期记忆方案.md](长期记忆方案.md) 八·M3「专家接入」第二个专家的落地设计。

---

## 〇、这份方案要回答的问题（TL;DR）

用户要用 **「另一个主 LLM」（模拟面试官）** 基于**用户简历 + 意向岗位 JD** 对用户逐轮提问；面试结束时调用一个工具生成**面试总结报告并落 `careers` 库**。核心判断：

1. **它不是一次性 tool**（区别于 `analyze/polish/create_career_plan`）：面试天然是多轮、有状态、跨消息的交互，必须做成**独立的会话型 agent 流程**——新增一套面试专属 session/消息/SSE 端点，而不是给现有助理 ALL_TOOLS 再塞一个工具。
2. **面试官主 LLM = 独立的 LLM 实例上下文 + 专属工具集**（复用现有 `get_student_profile` / `query_job_detail` + 新报告工具），SSE/trace/工具循环照搬 chat.py。
3. **报告落库走 Java career-service（`careers` 库新表）**，Python 经网关 HTTP POST 透传 JWT，不直连 careers 库——与 [职业规划专家方案.md](职业规划专家方案.md) 完全同构。
4. **面试过程（问答原文）与报告分离存储**：问答存 Python `chat_history`（新增表）；只有最终报告进 `careers`。报告由**独立的报告生成 LLM 子调用**基于「完整问答原文 + 简历 + JD」生成，不靠面试官从上下文里回忆总结（面试轮数多时上下文装不下、易漏）。

---

## 一、定位与输入输出

### 1.1 这是什么

一个**「模拟面试官」agent**：模拟 HR/技术面试官，读取**用户简历**与**意向岗位 JD**，以一问一答的节奏对用户提问（技术面 / 行为面 / 综合），到一定轮次或用户示意结束时，**生成一份面试总结报告（薄弱项 / 亮点 / 待补强点）**并持久化。

与既有专家定位的关系：

|       | analyze / polish / create_career_plan      | **模拟面试（本文）**                                         |
| ----- | ------------------------------------------ | ---------------------------------------------------- |
| 模式    | 同步、一次性、结构化 I/O 的 tool，一次 `ainvoke` 内自取数据完成 | **会话型、多轮交互**；同一场面试跨多条用户消息持续                          |
| 主 LLM | 主对话 LLM（assistant）决定何时调 tool               | **独立的面试官主 LLM**（专属 prompt + 专属工具集 + 专属 session 生命周期） |
| 落库    | 方案落 `careers`（Java）                        | 报告同样落 `careers`（Java）；问答过程落 `chat_history`（Python）   |

### 1.2 输入

| 输入       | 来源                                                                                                                                        | 何时取                  |
| -------- | ----------------------------------------------------------------------------------------------------------------------------------------- | -------------------- |
| 简历 id    | **用户在面试开场提供**（面试页选某份简历，或在对话里直接给 `profileId:xxx`）——**不默认取最新**                                                                              | 开场确认后，面试官据此 id 拉取    |
| 岗位 JD id | **用户在面试开场提供**（岗位卡「去模拟面试」带入，或对话里给 `jobId:xxx`）                                                                                             | 开场确认后，面试官据此 id 拉取    |
| 用户简历     | Java profile-service `GET /users/me/profile/{profileId}`（复用 `get_student_profile` / `resume_polish.fetch_profile`，**按用户给的 profile_id 取**） | 开场拿到 id 后由面试官调工具拉取一次 |
| 意向岗位 JD  | Java career-service `GET /jobs/{job_id}`（复用 `query_job_detail` / `resume_polish.fetch_job_detail`，**按用户给的 job_id 取**）                     | 同上                   |
| 面试类型     | `type`：`technical` / `behavior` / `mixed`（默认 `mixed`）                                                                                     | 建会话时指定               |
| 每轮用户回答   | 前端逐条发送                                                                                                                                    | 逐轮                   |

开场交互节奏：**这两样材料由用户提供**——开场时若已带 `profileId`/`jobId`（页面带入）则面试官先核对；缺哪个就先向用户索取。拿到 id 后面试官调绑定工具**按 id** 拉取简历 + JD，服务端把结果快照进会话（`material`），**之后的提问与报告都锚定这份材料**。用户中途说「简历我改过了，按新的来 / 换个 JD 考我」时，给出新的 id，面试官再调工具按新 id 重取（覆盖后续提问基准）。

### 1.3 产出

一份**面试总结报告** = 一篇 markdown 全文（`content`，内含总体评价 / 亮点 / 薄弱项 / 逐题简评 / 补强建议等小节），配套 `title` 与岗位快照列（`jobId`/`jobName`/`companyName`），落 `careers` 库 `user_interview_reports`（用户可跨会话回看列表/详情）。结构见 §四。

---

## 二、核心设计

### 2.1 形态：独立会话型 agent 流程，复用 chat.py 骨架

面试官与用户逐轮问答，**跨多条消息有状态**（进度、已问题目数、当前 resume/JD 基准、进行中/已结束）。chat.py 的 SSE + 工具循环是天然模板，但需要独立的：

- **session 生命周期**：`active`（进行中）→ `ended`（已出报告）。`ended` 后拒绝再发消息，只可读问答记录 / 报告。
- **专属系统 prompt**（面试官人设，替代助理 prompt）。
- **专属工具集** `INTERVIEW_TOOLS = [get_student_profile, query_job_detail, submit_mock_interview_report]`（不进 `ALL_TOOLS`）。
- **专属 transcript**：问答落 `interview_messages` 表（与助理 `messages` 表隔离，避免混入主对话会话列表）。

复用清单（从 chat.py 原样搬）：

| chat.py 段落                                                                      | 复用方式                 |
| ------------------------------------------------------------------------------- | -------------------- |
| `_sse()` / trace 事件协议（`start/trace/tool_call/tool_result/delta/done/error`）     | 照搬，前端可复用现成 SSE 解析    |
| `get_llm_with_tools(...)` 绑工具 + `tools_by_name`                                 | 改绑 `INTERVIEW_TOOLS` |
| `for _ in range(5)` 工具循环、身份/上下文强制注入（`user_id/token/request/session_id` 覆盖模型传入值） | 照搬                   |
| 工具结果 append 回 messages、末轮 `astream` 合帧输出                                        | 照搬                   |
| `_extract_thinking` / `_now_iso`                                                | 照搬                   |

### 2.2 简历 / JD 的取数方式：用户给 id → 面试官按 id 拉取 → 快照注入

材料 id **由用户在面试开头提供**，服务端**不在开场猜「最新一份简历 / 某个默认岗位」**。拉取动作发生在**开场那一轮**：面试官作为主 LLM 直接调用绑定工具 `get_student_profile(profile_id=...)` / `query_job_detail(job_id=...)` **按用户给的 id 取数**，本轮服务端捕获工具结果，快照进 `interview_sessions.material` 并回填 session 的 `job_id/profile_id`。

（之后每轮都让模型重调工具会重复拉取、费 token、上下文不持久，所以**拉一次即快照、后续每轮注入**：）

- **开场（start 流）**：上下文告知面试官「用户提供的 简历id=… / 岗位id=…」（缺哪个则先向用户索取）→ 面试官调工具按 id 拉取简历 markdown + JD → 服务端把取回结果快照进 `material`（`{"resume": "...", "job": {...}}`）。
- **每轮建 prompt**：`system` 注入面试官人设 + 材料分区（简历 + JD + 类型 + 已问 N/M 题），**保证每轮提问都锚在同一份事实上**。
- 材料就绪即进入正式提问；用户中途给新 id 换基准时，面试官再调工具重取并覆盖快照（覆盖本次之后的提问基准）。

### 2.3 一轮的推进逻辑（仿 chat.py 主循环）

每轮 = 一条用户回答 → 一次 SSE 流：

```
system: 面试官人设 + 【面试材料】简历/JD + 类型 + 进度（已问 x / 上限 M）
        + 硬规则：只问 1~2 个紧密相关的问题；不要一次抛出一堆题；到 M 题要主动提示可收尾；结束必须调用工具
history: interview_messages 全部原文（user/assistant 交替）
user:    本轮回答（若为开场则用不落库的合成指令）
        ↓ get_llm_with_tools(INTERVIEW_TOOLS) 最多 5 轮工具迭代
        · 面试官可调 get_student_profile / query_job_detail 按需补取（用户换基准）
        · 用户表示结束 / 已达上限 → 调 submit_mock_interview_report
        ↓ 无工具调用后 astream 生成最终内容
        · 平时 = 下一条提问；调过报告工具后 = 收尾语（转述报告要点，提示已保存）
        → 保存为本条 assistant 消息；若本轮调过报告工具 → 会话置 ended
```

**会话结束的触发**：模型判据 + 用户兜底。系统 prompt 要求面试官在（默认 8 题，可配）后主动询问「是否就此结束、出报告」；用户任何一轮直接说「结束面试 / 出报告」则模型应本轮调报告工具。服务端另留**硬上限**兜底：达 `interview_max_questions` 后 prompt 强制面试官进入收尾、只能调报告工具。

### 2.4 报告生成 = 工具内部的一次独立结构化 LLM 子调用（不是面试官自己总结）

面试到结束、面试官调 `submit_mock_interview_report` 时，**由工具内部**（`app/services/interview.py`）完成：

```
1) 读全量问答原文   interview_messages 该 session 全部（真实一问一答）
2) 取材料           material 快照（或按需重拉当前简历/JD，仍是「按 id 取」复用）
3) 独立报告 LLM     get_json_llm（关闭思考、低温度、结构化 JSON，对齐 plan._call_plan_llm），
                    输入 = 报告生成 prompt + 完整问答原文 + 简历 + JD
                    → 报告 JSON {title, content, jobId?/jobName?/companyName?}（见 §四）
4) 落库             content 已是整篇 markdown、无需二次渲染；
                    POST {career_service_base_url}/users/me/interview-reports（透传 JWT），
                    body = {title, content, sessionId, jobId?, jobName?, companyName?}
                    → careers.user_interview_reports，返回 reportId
5) 回写状态         面试会话 report_id / status=ended / ended_at
6) 返回给面试官      {reportId, title}，由模型向用户转述 + 收尾
```

为什么不直接让面试官「凭记忆」生成报告：面试轮数可能超过模型上下文，原文在每轮之间是分片出现的；工具侧读**数据库全量原文**再交给独立报告 LLM，保证报告覆盖整场、不臆造用户没说过的话。报告 LLM 的 prompt 写红线：只能基于问答原文与简历事实，用户没说/简历没有的回答不得虚构补全。

### 2.5 与主对话 assistant 的关系（已定：独立会话 + 独立页面/入口）

> 评审结论（2026-09-05）：**按「独立面试会话 + 独立页面/入口」做**。面试是独立会话，不进助理 `ALL_TOOLS`、不混入主对话会话列表；**主对话内直接切换角色**（同一会话内把 assistant 变成面试官）会造成 session 语义混乱，不做。

前端入口（M5）三个候选，至少落地前两个：

1. **岗位详情 / 匹配结果卡片上的「去模拟面试」按钮**——携带当前 `jobId` 跳转面试页，进入后由用户确认/选择用哪份简历（`profileId`，不默认取最新）再开场；这是最自然的入口。
2. **面试页内的「新开一场」**：进入面试页后选岗位（复用岗位搜索/选择组件）→ 选面试类型（技术/行为/综合）→ 开始。
3. （可选，后期）主对话 assistant 以一条带 route 的 action（复用 `HomeMessage.actions` 的 `navigate` 形态）把用户送进面试页，仅做跳转、不做角色切换。

---

## 三、数据存储

### 3.1 Python `chat_history` 库：两张新表（SQLAlchemy 模型，`init_db` 自动建）

面试过程数据跟随 Python 已连的 `chat_history`（`settings.database_url`），新增两个模型注册进 `Base.metadata`：

```
interview_sessions                       -- 一场模拟面试
  id            BIGINT UNSIGNED AUTO_INCREMENT PK
  session_id    VARCHAR(64) UNIQUE NOT NULL      -- 对外 id（gen_id）
  user_id       BIGINT NOT NULL                  -- 硬隔离
  job_id        VARCHAR(64) NULL                 -- 意向岗位 id（开场用户提供后回填）
  job_title     VARCHAR(200) NULL                -- 岗位名快照（列表展示/防删）
  company_name  VARCHAR(200) NULL                -- 公司名快照
  profile_id    VARCHAR(64) NULL                 -- 简历 id（开场用户提供后回填）
  report_id     BIGINT NULL                      -- careers.user_interview_reports.id
  created_at / updated_at / ended_at DATETIME
  KEY idx_interview_user_time (user_id, created_at)

interview_messages                    -- 一问一答原文（报告生成读取）
  id            BIGINT UNSIGNED AUTO_INCREMENT PK
  message_id    VARCHAR(64) UNIQUE NOT NULL
  session_id    VARCHAR(64) NOT NULL
  role          VARCHAR(16)                       -- user/assistant
  content       MEDIUMTEXT NOT NULL
  status        VARCHAR(20) DEFAULT 'succeeded'
  created_at    DATETIME DEFAULT CURRENT_TIMESTAMP
  KEY idx_interview_msg_session (session_id, created_at)
```

> 注：`init_db` 是 `create_all`，只建不存在的表、不改旧表——新表自动创建，无需 ALTER 既有 `sessions/messages`，助理侧零改动。

### 3.2 `careers` 库：报告表（Java career-service 建表出接口，对齐 `user_career_plans` 手法）

DDL 放 `career-service/src/main/resources/db/user_interview_reports.sql`，在 careers 库执行（`application-local.yaml` 的 `fc.db.database=careers`）：

```sql
CREATE TABLE IF NOT EXISTS user_interview_reports (
  id            BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '自增主键',
  user_id       BIGINT          NOT NULL COMMENT '用户ID（硬隔离）',
  session_id    VARCHAR(64)     NULL     COMMENT '来源面试会话 id（Python chat_history，溯源用）',
  job_id        VARCHAR(64)     NULL     COMMENT '意向岗位 id（快照）',
  job_name      VARCHAR(200)    NULL     COMMENT '岗位名快照（列表展示，防岗位被删）',
  company_name  VARCHAR(200)    NULL     COMMENT '公司名快照',
  title         VARCHAR(200)    NOT NULL COMMENT '报告标题（列表/入口展示）',
  content       LONGTEXT        NOT NULL COMMENT '报告 markdown 全文',
  created_at    DATETIME        NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  PRIMARY KEY (id),
  KEY idx_report_user_time (user_id, created_at)
) ENGINE = InnoDB DEFAULT CHARSET = utf8mb4 COMMENT = 'AI 模拟面试总结报告';
```

Java 落地文件（镜像 `CareerPlan` 一组）：`InterviewReport`(PO) / `InterviewReportMapper` / `InterviewReportService` / `InterviewReportController`（career-service `domain.po` / `mapper` / `service.InterviewReport` / `controller`）。报告是**每场一次**、不版本化，无需 `status/supersedes` 字段。

**网关路由：无需新增**——gateway `career-user`（`Path=/users/me/**`，order 3）已覆盖 `/users/me/interview-reports`；`/users/me/profile/**`（order 1）与 resume-parser 两条 order 0 路由都不吞此前缀。

### 3.3 与记忆/向量库的关系

面试报告、问答原文都**不进 pgvector**（无语义召回需求；只按 user 列表/详情访问）。长期记忆中面试结论的沉淀（如把薄弱项回写记忆）属可选增强，见 §六风险/取舍。

---

## 四、报告内容结构（简化：落库列对齐字段 + 一篇 markdown 正文）

报告由**独立的报告 LLM**（§2.4）基于「完整问答原文 + 简历 + JD」产出，输出为一份**与 Java 落库列对齐**的 JSON（不再引入维度数组等中间结构；正文直接是一篇含 markdown 语法的全文）：

```json
{
  "title": "Java 后端开发实习生 · 模拟面试报告（2026-09-05）",
  "content": "# Java 后端开发实习生 · 模拟面试报告\n\n## 总体评价\n…\n\n## 亮点\n…\n\n## 薄弱项\n…\n\n## 逐题简评\n…\n\n## 补强建议\n…（content 为整篇报告 markdown 原文，含 markdown 语法）",
  "job_id": "JOB2024...",
  "job_name": "Java 后端开发实习生",
  "company_name": "示例科技"
}
```

落库映射（Java `user_interview_reports` 列）：

| 报告 JSON 字段                             | 落库列       | 用途                                |
| -------------------------------------- | --------- | --------------------------------- |
| `title`                                | `title`   | 报告标题（列表 / 入口展示）                   |
| `content`                              | `content` | 报告 markdown 全文（详情展示 / 未来喂 prompt） |
| `job_id` / `job_name` / `company_name` | 同名快照列     | 列表展示；防岗位被删后仍可读                    |

约定与取舍：

- `user_id` / `session_id` 由服务端回填，**不由报告 LLM 产出**（身份不可由模型决定）。
- 报告正文 `content` 内按**固定小节**组织（总体评价 / 亮点 / 薄弱项 / 逐题简评 / 补强建议）——这些小节写进报告 LLM 的 prompt 保证结构稳定，但**不设结构化评分字段**（避免前端要展示还得二次解析）。若后续要与简历评分对齐成数值，再把某小节从「文本章节」提升为「JSON 字段」（见 §八 待定 2）。
- `job_id / job_name / company_name` 建议由**服务端从开场 JD 快照直接回填**（开场已按用户给的 id 拉过 JD），不依赖报告 LLM 复述；报告 LLM 只需保证 `title` 与 `content`。
- SSE trace 的「一句摘要」直接取 `title`（或 content 首段截断），不再单列 summary 字段。

---

## 五、对外接口

### 5.1 Python（前端 aiHttp 直连 8086，鉴权走 `security.py`；SSE 用 `?token=`）

路由前缀建议 `/users/me/interview`（对齐现有 `/users/me/home/assistant`）：

| 方法   | 路径                                                               | 说明                                                                                                                                   |
| ---- | ---------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| POST | `/users/me/interview/sessions`                                   | 建会话。body `{jobId?, profileId?, type?}`——id 缺省为空，**由用户在开场时提供**（页面带入或对话里给）。返回 `{sessionId, stream:{protocol:"sse", url:".../start"}}`  |
| GET  | `/users/me/interview/sessions/{sid}/start`                       | **开场 SSE**：面试官确认/索取用户提供的简历 id + 岗位 id → 按 id 调 `get_student_profile`/`query_job_detail` 拉材料 → 快照 material → 输出开场白 + 第 1 问（不落库伪造用户消息） |
| POST | `/users/me/interview/sessions/{sid}/messages`                    | 发用户回答。返回 `{sessionId, userMessage, stream}`（同 chat.py 协议）                                                                            |
| GET  | `/users/me/interview/sessions/{sid}/messages/{messageId}/stream` | 面试官 SSE：正常 = 下一问；用户结束 = 调报告工具 → 收尾语 → 会话 ended                                                                                       |
| GET  | `/users/me/interview/sessions/{sid}/messages`                    | 问答记录（可选，供「回看本场」）                                                                                                                     |
| GET  | `/users/me/interview/sessions`                                   | 面试会话列表（可选，`{id,title,type,status,createdAt}`）                                                                                        |
| POST | `/users/me/interview/sessions/{sid}/end`                         | （可选）用户主动点「结束」的兜底触发，等价于发一条「结束面试」                                                                                                      |

SSE 事件名与字段和 chat.py 完全一致（`start/trace/tool_call/tool_result/delta/done/error`），前端可直接复用现有 SSE 解析与气泡渲染。`_summarize_tool_result` 增加分支：`reportId` → 显示「已生成面试报告：{title}」。

### 5.2 Java career-service（前端走网关 `/api`，报告 CRUD）

`InterviewReportController`，`@RequestMapping("/users/me/interview-reports")`，沿用 common-service `Result{code,msg,data}` + `UserContext`：

| 方法     | 路径                                       | 说明                                                                                                             |
| ------ | ---------------------------------------- | -------------------------------------------------------------------------------------------------------------- |
| POST   | `/users/me/interview-reports`            | 创建报告（Python 报告工具调用）。body `{title, content, sessionId?, jobId?, jobName?, companyName?}`，返回 `{reportId, title}` |
| GET    | `/users/me/interview-reports`            | 列表：`{total, list:[{id,title,jobName,companyName,createdAt}]}` 倒序，不含 content                                    |
| GET    | `/users/me/interview-reports/{reportId}` | 详情：含 content；校验归属，非本人 404                                                                                      |
| DELETE | `/users/me/interview-reports/{reportId}` | 删除（可选）；归属校验                                                                                                    |

### 5.3 配置参数（config.py 新增块，命名对齐 plan_*）

```python
interview_max_questions: int = 8    # 提示性提问上限；到后 prompt 引导收尾出报告
interview_report_recent_messages: int = 0  # 预留：是否补读主对话上下文（本期不读，见风险）
```

---

## 六、风险与取舍

| 风险                             | 对策                                                        |
| ------------------------------ | --------------------------------------------------------- |
| 面试官每轮重复拉简历/JD（慢、费 token、上下文漂移） | 开场快照 `material` 每轮注入；工具只留作「换基准」兜底                         |
| 模型不结束、无限提问                     | 上限提示 + 达上限强制收尾；用户可任意一轮说「结束」；预留 `end` 兜底端点                 |
| 报告靠面试官凭记忆 → 漏题/臆造              | 报告工具内部读**库内全量问答原文** + 独立报告 LLM 结构化生成，prompt 红线：不得虚构       |
| 简历中途被用户改了                      | 面试基准 = 开场快照（同一场内部一致）；用户若要按新版重考，面试官调工具重取后从下轮生效（可提示重开一场更干净） |
| 长面试原文超上下文                      | 每轮只流式呈现当轮提问；报告生成读 DB 全量，不受单轮上下文限制                         |
| 报告/问答属于敏感个人数据                  | `user_id` 硬隔离 + 归属校验；问答原文仅存 `chat_history` 面试专属表、不喂公共检索   |
| 记忆层双写（薄弱项回写长期记忆）               | **本期不做**。若要，需对齐记忆抽取口径（仅用户主动认可时写），见风险 §5.4 of 职业规划专家方案     |
| 主对话 assistant 想一键把用户送进面试       | 用 action 卡片 `navigate` 跳面试页（带 jobId），**不做同会话角色切换**        |

---

## 七、里程碑

- **M1 Java 存储**：`user_interview_reports` DDL（`careers` 库）+ career-service `InterviewReport` PO/Mapper/Service/Controller（POST / GET / GET）；网关 `career-user` 已覆盖，不改路由。先于 Python 落库管线。
- **M2 Python 会话骨架**：`models.py` 加 `interview_sessions/interview_messages`；`interview.py` router 的建会话 / 开场 start SSE（面试官人设 → 确认/索取**用户提供的简历 id + 岗位 id** → 按 id 调工具拉材料 → 快照注入 → 开场白第 1 问）/ 消息落库 + SSE 协议。
- **M3 Python 工具循环 + 收尾**：绑 `INTERVIEW_TOOLS`（复用 `get_student_profile`/`query_job_detail`）；新增 `submit_mock_interview_report` 工具（读全量原文 → 独立报告 LLM → markdown → POST careers → 会话置 ended）；`interview.py` SSE 工具循环照搬 chat.py。
- **M4 健壮性/配置**：提问上限、`ended` 后拒收消息、错误兜底（报告 LLM 超时/落库失败返回明确 error、不产生假成功）、`_summarize_tool_result` 分支、config 参数。
- **M5 前端（在案）**：入口（岗位卡/匹配结果「去模拟面试」按钮 + 面试页内新开一场，见 §2.5）+ 独立面试页（复用 SSE/气泡）；「面试记录」页（对齐 `CareerPlanView.vue` 只读 markdown 查看器，GET `/users/me/interview-reports` 列表/详情）。
- 测试口径：建会话 → 开场第 1 问锚定简历+JD → 逐轮作答 → 触发结束 → 校验 `chat_history.interview_messages` 全量落库、`careers.user_interview_reports` 生成、会话 `ended` 且拒收新消息、报告 GET 列表/详情可读、非本人访问 404；报告内容不得出现用户未说/简历没有的事实。

---

## 八、已定与待定

1. **入口形态 —— 已定（2026-09-05）**：按**独立面试会话 + 独立页面/入口**实现（§2.5）。主对话内不做角色切换。
2. **报告评分口径 —— 暂按默认（简化版）**：报告正文按固定小节组织（总体评价 / 亮点 / 薄弱项 / 逐题简评 / 补强建议，写进报告 prompt，不设结构化评分列，见 §四），`title`+`content`+岗位快照字段落库。若要与简历评分/竞争力分对齐成数值（把某小节升级为 JSON 字段），在 M3 报告 prompt 定稿前可改，影响面小。
3. **实现顺序**：按 §七 M1→M5 推进。确认本方案后可先做 M1（Java 落库，无 Python 依赖），再做 M2/M3（Python 主链路）。
