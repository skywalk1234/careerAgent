ai-service-py/
├── main.py                        # FastAPI 入口（CORS + 启动建表）
├── requirements.txt
├── .env.example                   # 配置模板 → 复制成 .env 用
├── .gitignore                     # 已忽略 .env / *.db / .venv
└── app/
    ├── config.py                  # 配置：DeepSeek key / base_url / 模型名 / 数据库
    ├── database.py                # SQLAlchemy 异步引擎 + 自动建表
    ├── models.py                  # chat_session / chat_message 表（对齐 Java PO）
    ├── schemas.py                 # Pydantic 请求/响应模型
    ├── services/
    │   ├── llm.py                 # langchain-openai ChatOpenAI（懒加载）
    │   └── chat_service.py        # 会话/消息增查 + 组装 LLM 上下文
    └── routers/chat.py            # 4 个接口
