# 职业规划专家（行动方案）方案 v0.1

> 状态：**设计稿（v0.1，待评审）**
> 日期：2026-09-04
> 落点：生成在 `fc2026/ai-service-py`（`app/services/plan.py` + `app/tools.py` 第 7 个工具）；存储 / 查询在 **career-service（Java）**（`careers` 库新表，网关 `career-user` 已覆盖 `/users/me/plans`，无需新增路由）
> 存储决策（2026-09-04）：行动方案**不进向量库**，落 career-service 数据源连接的 MySQL `careers` 库新表，由 Java **career-service** 出接口；本期不做逐条勾选
> 上游依赖：长期记忆 M1/M2 已落地（`app/services/memory.py`）。本文是 [长期记忆方案.md](长期记忆方案.md) 八·M3「**专家接入**」的第一位专家。

---

## 一、定位与输入输出

### 1.1 这是什么

一个**「专家 tool」**（与 `analyze_resume` / `polish_resume` 同款定位）：由主 LLM 判断时机触发，**专家 service 内部自取数据、多阶段生成、落表持久化**，一次调用返回结构化结果。它不是常驻 agent，服务端无状态机（见 §2.1）。

触发场景：用户要「给我做个规划 / 接下来几个月怎么安排 / 想朝 XX 岗位，怎么一步步准备 / 秋招前时间怎么分配」。凡是**需要结合长期轨迹做中长期、分阶段安排**的，都交给它。

### 1.2 消费什么（三态 + 时间线 → 下一步动作）

| 输入                     | 来源（现状可用的读接口）                                                                       | 角色                    |
| ---------------------- | ---------------------------------------------------------------------------------- | --------------------- |
| 简历画像                   | Java profile-service（`resume_polish.fetch_latest_profile` / `_load_resume`，默认最新一份） | **事实底座**（结果态：你有什么）    |
| 长期记忆 current-view      | `memory.load_current_view`（每核心类最新一条活跃）                                             | 现在坐标：学到哪、卡在哪、目标       |
| 长期记忆 **timeline-view** | ⚠️ **待补**：现无此读接口（见 §四 M1）                                                          | 来路：进步速度 / 卡点演变 / 目标漂移 |
| 长期记忆 语义召回（可选）          | `memory.search_episodes`                                                           | 按主题补细节                |
| 目标岗位 JD（可选）            | career-service（`resume_polish.fetch_job_detail`，有 `jobId` 才取）                      | 目的地（想投什么）             |
| 历史对话                   | **不整段灌入**，由「记忆投影 + 主模型上下文」承担（见 §2.3）                                               | —                     |

### 1.3 产出什么

一份**行动方案（action plan）**：分阶段、带目标与检查点的完整建议文档。**落表持久化**（`user_id / title / content ...`，见 §三）到 MySQL `careers` 库（career-service 连接库），跨会话可回看、可演进——再次规划产出新一版、旧版归档。逐条勾选跟踪本期不做（v2 可加）。

---

## 二、核心设计判断

### 2.1 同步、一次性、结构化 I/O 的 tool，不做常驻 agent

与 `analyze` / `polish` 一致：工具被调后**内部**完成「取数 → 一次生成方案 → 经网关落库 → 返回」，一次 `ainvoke` 内结束。好处：

- SSE / trace / 工具循环 / 失败兜底全部复用现成链路，只新增一个 `@tool` + 一个 service 文件；
- 生成阶段之间可以用独立 LLM 调用（Reflection），但都发生在这一次调用里，无跨请求状态。

### 2.2 输入「谁取数」：专家内部自取，不靠主模型先取完再塞参数

若要求主模型先把简历+记忆+JD 全部取来再塞进一个参数，会：参数爆炸、主模型上下文受限易漏、还依赖模型记得先调 `recall_memory`。所以**对齐 analyze/polish 内部自拉材料**的做法——工具对外参数保持最少，主模型只决定「要不要规划 / 为哪个方向 / 多长周期」：

```
create_career_plan(
    target: str | None,      # 目标岗位/方向。消息里有 jobId:xxx 或岗位名则从对话抽，可省略
    profile_id: str | None,  # 用哪份简历；默认最新一份
    focus: str | None,       # 可选约束：如「想重点补算法」「秋招前」
)
```

身份/上下文（`user_id / token / request / session_id`）由 [chat.py](fc2026/ai-service-py/app/routers/chat.py) 现有工具循环强制注入，工具**不可信模型传入的身份**——与 `recall_memory` 一致。

### 2.3 历史对话怎么进专家

**不整段读 chat_history 灌 prompt**（太噪、超长）。专家读「记忆投影」承担"历史"：

- current-view → 现在坐标
- timeline-view（M1 补）→ 演进轨迹
- 个别主题不清晰 → 语义召回兜底

一个窗口问题：记忆抽取是**每轮后台**触发，紧邻当前回合通常已就绪，但仍有刚说完还没沉淀的空档（用户这条消息刚描述了新项目进展）。折中：**可选读「本会话最近 N 条原文」**作即时速览，`config.plan_recent_messages`（默认 8），标低优先、不得覆盖长期记忆。

### 2.4 方案如何演进：版本化，不覆盖（呼应记忆 supersede 思想）

行动方案会随用户进展过时——执行两周后再规划，应产出**新一版**而不是覆盖旧版。参照 `user_episodic_memory.superseded_by_id`：旧方案**不物理删**（历史快照 = 用户成长的另一种时间线载体），保存新方案时把上一条 active 置为 `archived`（或加 `supersedes_plan_id` 保留版本链）。查询「当前激活方案」= `status='active'` 最新一条。列表/详情按需给 archived。

### 2.5 与长期记忆的双向关系：消费方 +（可选）回写方

- **消费**：见 §1.2。
- **回写（可选增强，见 §五 5.4）**：方案本身也是未来的记忆——激活方案经用户认可后，方案目标应成为常驻 current-view「目标」栏的最新内容（supersede 旧 goal），让下次对话助手天然带着「你正在执行第 1 阶段」回话。这依赖「用户认可」语义，与记忆抽取口径（§四：用户陈述是唯一事实源）需对齐，机制细节列待定。

---

## 三、存储设计：一张新表（MySQL `careers`，Java 落库）

### 3.1 放哪：career-service 的 MySQL `careers` 库，不进向量库

行动方案**不进向量库**——它是普通结构化持久化产物，只按 `user_id` 做列表 / 详情访问，无需语义召回；真正需要向量的是长期记忆（过程态轨迹，留 pgvector）。方案归「职业规划」领域，跟随 career-service 的 per-user 持久数据（职业路径 / 生涯报告 / 岗位收藏），落到 **career-service 数据源连接的 MySQL `careers` 库**（`application-local.yaml` 的 `fc.db.database=careers`，192.168.118.130，2026-09-04 已建表；profile-service 连的是另一个库 `user_profile`，两者不同，方案表只归 career-service），由 **career-service（Java）建表并出接口**：

- Python 专家只负责**生成**，经网关 HTTP 把方案 POST 给 career-service 落库（透传 JWT），**不直连该库、不经 pg 连接池**；
- 前端「我的方案」经网关 `/api` 读取（Java 链路），Python 不新增 plans router；
- 历史方案只做列表 / 详情查询，**无向量列、不做语义检索**。
- Java 落地文件：`CareerPlan`(PO) / `CareerPlanMapper` / `CareerPlanService` / `CareerPlanController`（career-service `domain.po` / `mapper` / `service.CareerPlan` / `controller`）；建表 DDL 见 `career-service/src/main/resources/db/user_career_plans.sql`。

### 3.2 表：方案主表（career-service 落库）

```
user_career_plans（MySQL careers；用户可多次规划，新版本插入时旧版置 archived 不删）
  id            BIGINT UNSIGNED AUTO_INCREMENT PK   -- 对齐 resume_full / ability_score 主键风格
  user_id       BIGINT           -- 硬隔离，绝不可少（方案属敏感数据）
  session_id    VARCHAR(64)      -- 来源会话，溯源用（可空）
  status        VARCHAR(16)      -- active / archived（同 user 最新一条 active = 当前方案）
  title         VARCHAR(200)     -- 列表 / 入口展示
  content       LONGTEXT         -- 方案 markdown 全文（详情展示 / 未来喂 prompt）
  supersedes_plan_id  BIGINT NULL -- 版本链：本方案取代了哪份旧方案（可空）
  created_at    DATETIME DEFAULT CURRENT_TIMESTAMP
  KEY idx_plan_user_status (user_id, status, created_at)
```

## 四、规划专家内部流程（`app/services/plan.py`）

**单次结构化生成，一遍过**：参照 [resume_polish.py](ai-service-py/app/services/resume_polish.py) 的 `_call_json_llm` + `asyncio.wait_for` 硬超时模式，一次 LLM 调用生成方案，不做 REVIEW/REFINE 评审修正循环（v0 取舍；红线直接写进生成 prompt，后续要精修再引入独立评审）。

```
0) 取数（并行拉取）：
    简历全文（_load_resume，profile_id 或最新）        → 唯一事实底座
    memory.load_current_view(pool, user_id)            → 现在坐标
    memory.load_timeline_view(pool, user_id)           → 演进轨迹（M1 新增）
    可选 job_id → resume_polish.fetch_job_detail(...)  → 目的地 JD
    可选本会话最近 N 条（config.plan_recent_messages） → 即时速览
1) 生成（单次 LLM，一遍过）：源材料 → 方案 JSON，并当场拼成整份 content markdown
    {title, goal, diagnosis, gapList[], phases:[{name, objective,
      actions[], dueLabel}], risks[], checkpoint}
    - 红线直接写进生成 prompt（见下），不做 REVIEW/REFINE 独立评审修正（v0 取舍）
    - 生成失败 / 关键字段缺失 → 返回 error，不继续落库
2) 落库：经网关 POST 把 content markdown 交给 career-service（`{career_service_base_url}/users/me/plans`，透传 JWT），返回 planId；
    「新方案取代旧版」由 Java 侧在插入时原子完成（上一条 active 置 archived，
    或填 supersedes_plan_id）。落库失败 → 整次调用返回 error，不产生半成品 / 假成功
3) 返回 {"planId","title","goal","summary","markdown"}
    markdown=完整方案（主模型读后向用户转述要点 + 提示已保存）；
    summary=可读一句摘要（供 SSE trace）
```

红线（写进 PLAN_SYSTEM，对齐记忆 §1.2「简历事实底座 / 记忆是侧写」）：

- 规划只能基于简历、记忆、JD 呈现的事实；**动作拆解可推断，事实不可虚构**；
- 「正在学 / 想转」类记忆用于分期与排序，不能当成已会的能力写进 gap 结论；
- 不给精确承诺（offer/薪资/周期内一定上岸），给的是「做什么、按什么顺序、怎么自检」。

工具名建议：`create_career_plan`，docstring 写明触发时机与 `target/profile_id/focus` 参数抽取规则（对齐 `polish_resume` docstring 风格），追加进 `ALL_TOOLS`。

---

## 五、接线与对外接口

### 5.1 chat 主链路：几乎零改动

- [tools.py](ai-service-py/app/tools.py)：新增第 7 个工具 `create_career_plan` + 追加进 `ALL_TOOLS`；
- [chat.py](ai-service-py/app/routers/chat.py)：`_summarize_tool_result` 加一个分支——看到 `planId` 返回「已生成行动方案：{title}」，让前端 trace 面板显示清晰摘要；身份注入逻辑已通用，无需改。

### 5.2 存储 / 查询接口：Java career-service（经网关，前端走 `/api`）

方案的新建 / 列表 / 详情都在 career-service（新增 `CareerPlanController`，`@RequestMapping("/users/me/plans")`），沿用 common-service 的 `Result{code, msg, data}` / `UserContext` 取 userId 约定（网关 JWT 校验负责鉴权）。**接口细节、表结构、调用示例见 [行动方案接口文档.md](行动方案接口文档.md)**：

| 方法   | 路径                         | 说明                                                                                                                                                |
| ---- | -------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| POST | `/users/me/plans`          | 创建方案（Python 专家生成后调用）。body `{title, content, sessionId?}`；插入新行并把该用户上一条 `active` 置 `archived`（或填 `supersedes_plan_id`），返回 `{planId, title, status}` |
| GET  | `/users/me/plans`          | 列表：`{total, list:[{id, title, status, createdAt}]}`，按时间倒序（当前方案 = 最新一条 active，不含 content 正文）                                                                                   |
| GET  | `/users/me/plans/{planId}` | 详情：`{id, title, status, content, supersedesPlanId, createdAt}`；校验归属（非本人返回 404）                                                                  |

**网关路由**：**无需新增**——gateway 的 `career-user` 路由（`Path=/users/me/**` → career-service，order 3）已覆盖 `/users/me/plans` 前缀；`/users/me/profile/**` 走 order 更前的 profile-service 路由，前缀不冲突，不会被吞。

**Python 侧**：`plan.py` 只经网关 HTTP POST 落库——`settings.career_service_base_url`（默认网关 8080）+ `/users/me/plans`，透传 JWT，写法参照 `resume_polish.save_profile_as_new`（httpx）——**不新增 plans router、不改 DB 配置**；生成的方案 JSON 拼成 `content` markdown 作为 body 发送。

### 5.3 配置参数（config.py 新增块，对齐记忆参数命名）

```
plan_recent_messages: int = 8      # 专家补读本会话最近 N 条原文（即时速览）
plan_timeline_rows: int = 12       # timeline-view 取最近活跃条数
```

### 5.4 记忆回写（可选增强，待定）

保存方案后，把方案的 `goal` + 当前首阶段提炼成一条 `category='goal'` episode 写入 pgvector 长期记忆（supersede 旧的 goal），使记忆 current-view「目标」栏指向最新行动方向——方案正文存 MySQL、目标记忆留向量库，两个存储各自独立，靠 `user_id` 关联。**前提是对齐记忆抽取口径**：仅当「规划是用户主动提出 / 用户对方案表态认可」时才写，避免模型单方产出冒充用户意图。v0 可先不做，靠下次普通对话 + 抽取器自然沉淀。

---

## 六、风险与取舍

| 风险                                      | 对策                                                                 |
| --------------------------------------- | ------------------------------------------------------------------ |
| 记忆落后于现状（刚说完还没沉淀）                        | 简历实时拉取 + 可选读本会话最近 N 条 + current-view 每轮刷新                          |
| 专家把「想学/在学」写成「会」                         | 生成 prompt 红线 + PLAN_SYSTEM 明示 + gap 需对得上 JD/自评                      |
| 方案过时（目标漂移）                              | 版本化：新方案 archive 旧方案，读 active 最新；旧版可回看成长轨迹                          |
| 单次生成方案内容长、耗时长                            | `asyncio.wait_for` 硬超时兜底（复用 polish）+ 耗时日志；行动项多时 max_tokens 上调        |
| 工具返回 markdown 偏大（进工具结果上下文）              | summary 精炼给主模型转述，markdown 为主模型讲细节可截断提示已落库可看全文                      |
| 生成(Python) 与落库(Java career-service) 跨栈 | plan.py 经网关 POST 落库，失败返回明确 error、不产生半成品；active 归档由 Java 插入时原子完成    |
| 与 Java「生涯报告/职业路径」语义重叠                   | 定位错开：AI 会话内实时行动方案 vs 导出报告；后续可把方案沉淀喂给 Java 导出                       |
| 敏感数据                                    | `user_id` 硬隔离 + 用户可删；仅存方案本体不存无关隐私                                  |

---

## 七、里程碑

- **M1 记忆读接口补齐**：`memory.load_timeline_view(pool, user_id, limit, categories)`（timeline-view 投影，按 category/时间取最近活跃、保留演进顺序，见 [长期记忆方案.md](长期记忆方案.md) §3.2 视图 B）——现只有 current-view / search / recent，**缺轨迹视图**，这是专家的核心输入，先补。
- **M2 Java 侧存储**：`user_career_plans` DDL（`careers` 库，见 `career-service/src/main/resources/db/user_career_plans.sql`）+ career-service 的 `CareerPlan` PO/Mapper/Service/Controller（POST / GET / GET 三接口）；网关 `career-user` 已覆盖 `/users/me/plans`，无需改路由。先于 Python 管线，落库才有去处。
- **M3 生成管线**：`app/services/plan.py`（取数 → 一次生成 content → 经网关 POST 落库）+ `create_career_plan` 注册 + `_summarize_tool_result` 分支。
- **M4 前端**（可选）：对话内生成 + 「我的方案」页（经 `/api` 读 Java）。
- 测试口径：真实对话触发规划 → 校验 `careers.user_career_plans` 落库、同 user 仅一条 active、再次规划旧版置 archived、前端列表 / 详情可读；落库失败时工具返回明确 error 而非假成功。

> 依赖记忆层的定位不变：**记忆层先于专家存在，专家以它为主输入**；本专家落地后，「工具化 → 陪伴化」的闭环形成——用户下次回来，助手带着「你正执行当前方案的第 1 阶段」接话（靠记忆 current-view 中的 goal + `careers` 库方案表 active 记录支撑）。
