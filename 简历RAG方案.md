# 简历润色 RAG 方案（优秀点评参考）

> 状态：总体规划稿（V1）　日期：2026-09-02
> 面向场景：`polish_resume` 润色时，检索小红书等平台的**优秀简历点评**作为「判断标准 / 改写思路」参考，提升项目描述的技术深度与差异化，同时守住「不虚构」红线。

---

## 一、背景与目标

### 1.1 现状与痛点

当前 `polish_resume`（[app/tools.py](ai-service-py/app/tools.py)）的润色链路是：

```
JD(可选) + 简历原文 + 用户补充信息 → 专家提示词 → LLM 润色 → 另存新简历
```

润色效果完全依赖提示词里的通用规则（突出岗位匹配、量化成果等），缺少**同类岗位 / 同类项目「怎么写才显深度」的具体参考**。常见的简历弱点是：项目描述堆砌技术名词、套开源库模板、看不出设计思考——这类问题靠通用规则很难改好。

### 1.2 方案切入点

调研中发现：**小红书简历点评帖下，高赞评论往往比帖子本身更有参考价值**。评论不是简历，而是专家对具体简历项目的批判性分析，例如：

- 「Runtime 层 / 协作层是行业基础操作，看不出水平，可略写」
- 「评测与回放层很有意义：证据系统提升长程任务质量，回放层符合最小改动原则」
- 「最亮眼的是产品化落地：做垂类 Roles/Skill 走专家知识沉淀，避开与主流工具正面竞争」
- 「长期记忆和 RAG 面临同样的召回问题……图谱+向量，一个建逻辑一个建语义」

这类内容给出的是**可迁移的判断标准与改写思路**（什么算技术深度、如何定位项目、哪些设计值得写），恰好是润色时最缺的输入。

### 1.3 目标

- 为润色阶段引入 RAG，按「用户要打磨的项目 / 技术方向」检索相关点评片段，注入提示词作为**参考视角**。
- 明确与简历范本 RAG 的区别：借鉴**标准与思路**，禁止挪用点评中提到的**具体项目特性**（红线）。
- 语料、表结构、检索、接入形成一套可落地的总体规划，分阶段实施。

---

## 二、总体设计

```
                    ┌─────────────── 灌库（离线，一次性/增量）───────────────┐
 小红书帖子评论 ──→ 人工筛选(curated) ──→ 按「一个观点」分片 ──→ 嵌入(text-embedding-v1)
                    └──────────────┬───────────────┬───────────────┘
                                   ▼               ▼
                         resume_example_vector   metadata(techField / dimension / 来源)
                                   ▲
                    ┌──────────────┴─────────────── 检索（在线）────────────────┐
                    │  query = 目标岗位方向 + 用户简历里要打磨的项目/经历原文        │
                    │  → techField(±dimension) 过滤 → cosine top_k 3~5 条        │
                    └──────────────┬───────────────────────────────────────────┘
                                   ▼
   polish_resume ──→ resume_polish.polish() ──→ 【参考点评视角】注入 → LLM 润色
```

**核心思路一句话**：向量库不存「优秀简历」而存「优秀点评」，检索不以「岗位」为单位而以「项目 / 技术方向」为单位，让润色专家学到的是**评判项目的视角**而不是**别人的经历**。

---

## 三、数据层设计

### 3.1 向量库与表

- **位置**：真实/线上向量库 `8.147.71.59:40086/ai-vector`（即 `ai-service-py/config.py` 的 `vector_database_url` 指向，与 `job_category_vector` / `job_detail_vector` 同库）。
- **嵌入**：复用 DashScope `text-embedding-v1`（1536 维），与岗位表同维度，才能复用现有 `similarity_search`。
- **表**：`resume_example_vector`，与 `job_detail_vector` 同构（Spring AI PgVectorStore 标准 schema）。`metadata` 用 `JSON` 类型（对齐 `similarity_search` 中 `json.loads(r["metadata"])` 的解析）。

```sql
CREATE TABLE IF NOT EXISTS resume_example_vector (
    id UUID NOT NULL,
    content TEXT,                 -- 点评片段文本
    metadata JSON,                -- 见 3.3
    embedding VECTOR(1536),
    PRIMARY KEY (id)
);

CREATE INDEX IF NOT EXISTS resume_example_vector_embedding_idx
    ON resume_example_vector
    USING HNSW (embedding vector_cosine_ops);
```

> 提醒：请确认在建在**真实库**而非本地开发 PG（`192.168.118.130`），与 CLAUDE.md 中「两套向量库指向」的坑一致。

### 3.2 content：存点评片段，不存整篇评论

一条长评论往往横跨多个项目、多个维度，整条嵌入会让语义互相稀释，检索时「用户有个 RAG 项目」命中不到「讲 RAG 召回设计」那句。因此按**一个完整观点**切成一条（几十到一两百字为宜）：

```
片1  「先说个结论：Coding Agent和RAG从来都不是不能写……堆砌技术名词没意义，一切落到具体项目实现」
片2  「Runtime层和协作层是行业基础操作，看不出水平高低，可以略写」
片3  「评测与回放层很有意思：证据系统提升长程任务质量（谷歌论文佐证）；回放层符合最小改动原则……」
片4  「最亮眼的是产品化落地……做垂类Roles/Skill走专家知识沉淀，避开和主流Coding工具的正面竞争」
片5  「长期记忆和RAG面临同样的召回问题……图谱+向量：一个建逻辑一个建语义……」
```

### 3.3 metadata 字段

从「简历身份属性」改为「点评内容属性」，核心是 **techField（过滤主维度）+ dimension（可借鉴的方面）+ 来源**：

| 字段 | 说明 | 示例 |
|---|---|---|
| `techField` | 评论涉及的技术方向（检索主过滤维度） | `Agent开发`、`RAG`、`Java后端` |
| `dimension` | 点评维度 | `技术深度` / `项目定位` / `产品化` / `设计思想` / `写作技巧` / `常见误区` |
| `level` | 面向的求职层次（可选） | `校招`、`社招` |
| `postId` | 来源帖子 ID | — |
| `postTitle` | 来源帖子标题（供上下文参考） | `XX简历点评` |
| `author` | 作者昵称 | — |
| `sourceUrl` | 来源链接 | `https://…` |
| `curated` | 是否人工筛选过的优质点评（质量门禁） | `true` |

`dimension` 可与 `analyze_resume` 的诊断联动：分析指出「技术深度不足」→ 过滤 `dimension=技术深度` 的点评来补。

---

## 四、语料建设

### 4.1 数据来源与筛选

- 从小红书简历点评帖（含高赞评论）人工收集；初期**只灌筛选过的优质点评**（`curated=true`），低质点评会反向带歪润色结果。
- 质量门槛建议：观点具体（指出「为什么好 / 为什么差」）、可迁移（不是针对某一条个人经历）、无时效性过强内容。
- 起步量级：数十条覆盖主流技术方向即可验证，逐步扩充。

### 4.2 整理与分片规则

- 每条评论按「一个完整观点」手工/半自动切分，去掉废话与个性情绪。
- 每个分片补全上下文使其可独立阅读（必要时在片首加主题词，如「关于Agent项目：」）。
- 标注 `techField` / `dimension` / 来源；写入前人工复核。

### 4.3 灌库脚本（规划）

参照 [scripts/import_intern_jobs.py](ai-service-py/scripts/import_intern_jobs.py) 的既有模式：

- 数据以结构化文件维护（如 `scripts/resume_comments_data.py` 或 JSON），脚本负责分片 → `embed_text` → 写表。
- 提供 `--dry-run`（只构建数据与向量、不写入）与幂等写入（按 `postId+分片序` 判重/覆盖）。
- 复用 `config.py` 的向量库与嵌入配置，保证与检索端同库同模型。

---

## 五、检索设计

### 5.1 查询文本构造

不再用「岗位 JD」作为唯一 query，而是**用户自己的项目描述 + 目标岗位方向**：

```
query = 目标岗位方向/关键词（粗定位）
      + 用户简历里要重点打磨的那个项目/经历的原文（精定位）
```

例如：用户简历里有「开发了基于 RAG 的智能问答系统」，embedding 这段，应命中「RAG 召回 / 动态记忆 / 图谱+向量」类点评，而不是「Runtime 层」类。

### 5.2 过滤与排序

- 先按 `metadata.techField` 过滤（可加 `dimension` 第二重过滤），再做 cosine 相似度排序，取 `top_k = 3~5` 条。
- 阈值：点评片段间的相似度天然低于岗位匹配，**阈值应放宽**，靠 `techField` 过滤兜底，不纯靠相似度卡。
- 无命中或纯通用改写时**跳过 RAG**，保持现有行为（RAG 只增强、不阻塞）。

### 5.3 预留参数

在 `config.py` 增加一组独立参数（如 `resume_example_top_k`、`resume_example_threshold`），与岗位推荐参数分离，便于调优。

---

## 六、业务接入（润色流程）

### 6.1 接入位置

- `polish_resume`（tools.py）：在拉取简历 / JD 后，构造查询文本 → 检索 → 把命中片段作为新参数传入 `resume_polish.polish()`。
- `resume_polish.polish()`：user prompt 增加一段「参考点评视角」，只保留文本与来源标注（`techField / dimension / postTitle`），不带敏感信息。
- `vector_store.py`：表名白名单增加 `TABLE_RESUME_EXAMPLE`，检索函数其余逻辑不变。

### 6.2 提示词注入与红线

向 `POLISH_SYSTEM` 或 user prompt 明确声明：

1. **借鉴的是判断标准与改写思路**：怎么把项目写得有深度、怎么定位避开同质竞争、哪些设计逻辑值得强调。
2. **严禁照搬/安插**：点评中提到的**具体项目特性**（如「评测与回放层」「证据系统」）只能作为理解依据，**绝不虚构进用户简历**；沿用现有「保留原事实、不编造」红线。
3. **保留原简历事实**：只重组、改写、扩写用户已提供的内容，与现有红线一致。

> 与「存简历范本」方案相比，点评类语料更安全：它本身是标准/思路而非经历，天然与「不虚构」红线兼容。

---

## 七、质量与风险

| 风险/注意点 | 对策 |
|---|---|
| 低质点评带歪结果 | `curated` 门禁，只灌筛选过的；必要时在检索时过滤 |
| 模型挪用点评中的具体项目特性 | 提示词红线 + 复核输出（沿用 reflect 自检） |
| 注入过多挤占生成空间 | 3~5 条、每条数百字内；超长评论先行分片 |
| 命中不相关 | `techField` 过滤 + 放宽阈值，宁缺毋滥 |
| 向量库建错位置 | 确认建在真实库 `8.147.71.59:40086`，与 `config.py` 一致 |
| `similarity_search` 表名白名单 | 扩展 `vector_store.py` 允许表常量，杜绝注入 |

---

## 八、实施计划

| 阶段 | 内容 | 产出 |
|---|---|---|
| P1 基建 | 建表（真实库）+ `vector_store.py` 白名单扩展 + config 参数 | 表就绪，检索函数可查新表 |
| P2 语料 | 整理首批点评数据（分片 + metadata 标注） + 灌库脚本（dry-run 优先） | 首批 `curated=true` 语料入库 |
| P3 接入 | `polish_resume` 构造 query → 检索 → 注入 `polish()` + 提示词红线 | 润色链路支持 RAG |
| P4 验证 | 目标岗位定向润色，对比有无参考的输出；无虚构校验 | 效果评估与阈值调优 |

## 九、验证方式

- 灌库：`--dry-run` 检查分片/元数据/嵌入，再正式写入；按 `postId` 幂等。
- 检索：抽查指定技术方向（如 Agent / RAG）能否命中对应点评片段。
- 润色：同一份简历 + 同一岗位，开启/关闭 RAG 对比输出，重点看项目描述的技术深度与差异化，并人工核验无虚构。
