# 行动方案（user_career_plans）接口文档

> 本文档描述 **AI 职业规划专家**生成的「行动方案」在 Java 侧的**存储 / 查询接口**，实现于 **career-service**。
> 设计背景见 [职业规划专家方案.md](职业规划专家方案.md)：Python 专家（`fc2026/ai-service-py`）只负责**生成**，方案内容经网关 HTTP POST 到本服务落库；前端「我的方案」也走本服务读取。
> 相关代码：`career-service/src/main/java/group/careerservice/` 下的 `controller/CareerPlanController`、`service/CareerPlan/CareerPlanService`、`mapper/CareerPlanMapper`、`domain/po/CareerPlan`、`domain/dto/CareerPlanCreateDTO`；建表 DDL 见 `career-service/src/main/resources/db/user_career_plans.sql`。

## 模块概览

| 项 | 说明 |
|---|---|
| 服务名 | `career-service`（Java 17 + Spring Boot + MyBatis-Plus） |
| Controller | `CareerPlanController`，前缀 `@RequestMapping("/users/me/plans")` |
| 数据库 | MySQL `192.168.118.130` 的 **`careers`** 库（`application-local.yaml` 的 `fc.db.database=careers`），表 `user_career_plans`（2026-09-04 已建） |
| 设计文档 | [职业规划专家方案.md](职业规划专家方案.md) §三 / §五.2 |
| 对外访问 | 前端一律经网关 `http://localhost:8080`（JWT 校验）；Python 专家同样经网关、**透传 JWT** |

> ⚠️ 表只在 career-service 数据源连接的库里（当前 = `careers`）。**profile-service 连的是另一个库 `user_profile`**，方案表与其无关。若某环境 career-service 数据源指向别的库，需在对应库执行 DDL。

### 网关路由（无需新增）

网关 `career-user` 路由（`Path=/users/me/**` → career-service，order 3）**已覆盖** `/users/me/plans` 前缀。而 `/users/me/profile/**` 命中 order 更前的 `profile` 路由（order 1，profile-service），两者前缀不冲突，改路由时注意不要打乱该顺序。

### 认证与用户身份

- 所有 `/users/me/**` 路径经网关 JWT 校验后放行；
- Controller 通过 `UserContext.getUser()`（ThreadLocal，由网关透传）取当前登录用户 id；理论上必不为空，代码按 career-service 惯例兜底回退 `111`；
- **归属校验**：detail / list 一律按 `user_id` 过滤，方案硬隔离——越权读不到别人的方案。

### 统一响应格式

所有 HTTP 接口返回 `{code, msg, data}` 三层结构（`group.common.Result<T>`）：

```json
{ "code": 200, "msg": "success", "data": { ... } }
```

- `code=200` 成功；业务错误 `400`（参数错误）、`404`（不存在 / 无权）、`500`（服务端错误）。
- 下文只描述 `data` 字段结构。

---

## 一、接口总览

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/users/me/plans` | **创建行动方案**（Python 专家生成后调用）。同用户上一条 `active` 自动置 `archived` 并回填版本链 |
| GET | `/users/me/plans` | **方案列表**（不含正文，按时间倒序），返回 `{total, list}` |
| GET | `/users/me/plans/{planId}` | **方案详情**（含 `content` 全文），越权返回 404 |

---

## 二、数据模型

### 表 user_career_plans（MySQL `careers` 库）

```sql
CREATE TABLE IF NOT EXISTS user_career_plans (
  id                  BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '自增主键',
  user_id             BIGINT          NOT NULL                COMMENT '用户ID（硬隔离，绝不可少）',
  session_id          VARCHAR(64)     NULL                    COMMENT '来源会话 id（溯源用，可空）',
  status              VARCHAR(16)     NOT NULL DEFAULT 'active' COMMENT 'active=当前方案 / archived=历史版本',
  title               VARCHAR(200)    NOT NULL                COMMENT '方案标题（列表/入口展示）',
  content             LONGTEXT        NOT NULL                COMMENT '方案 markdown 全文',
  supersedes_plan_id  BIGINT          NULL                    COMMENT '版本链：本方案取代的旧方案 id（可空）',
  created_at          DATETIME        NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  PRIMARY KEY (id),
  KEY idx_plan_user_status (user_id, status, created_at)
) ENGINE = InnoDB DEFAULT CHARSET = utf8mb4 COMMENT = 'AI 职业规划专家生成的行动方案';
```

### 版本化语义（重要）

- **当前方案** = 该用户 `status='active'` 的最新一条；历史快照置 `archived`，**不物理删除**（可回看成长轨迹）。
- **版本链**：新方案插入时，若用户已有 active 方案，则把其 id 写入新行的 `supersedes_plan_id`，再把旧行置 `archived`。
- 上述两步在 `CareerPlanService.createPlan` 的**一个 `@Transactional` 事务**内原子完成，不会出现「新方案已落但旧版未归档」的半成品状态。
- 找「当前 active」用 `ORDER BY created_at DESC, id DESC LIMIT 1`，避免 `selectOne` 因多条 active 抛异常。
- 本期**不做逐条勾选**：`content` 存整份 markdown 全文。

---

## 三、接口详情

### 3.1 POST /users/me/plans — 创建行动方案

**用途**：Python 职业规划专家生成方案后，把 content markdown 经网关 POST 到这里落库；也可由前端直接提交（一般不用）。

**请求体**（JSON）：

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `title` | string | 是 | 方案标题，≤200 字符（列表 / 入口展示用） |
| `content` | string | 是 | 方案 markdown 全文（LONGTEXT） |
| `sessionId` | string | 否 | 来源会话 id，溯源用 |

```json
{
  "title": "2026 秋招 · 后端开发三个月行动方案",
  "content": "## 目标\n...（整份 markdown）...",
  "sessionId": "conv_20260904_abc123"
}
```

**成功响应**（`code=200`，`msg="行动方案已保存"`）：

```json
{
  "code": 200,
  "msg": "行动方案已保存",
  "data": {
    "planId": 1001,
    "title": "2026 秋招 · 后端开发三个月行动方案",
    "status": "active"
  }
}
```

**错误响应**：

| code | msg | 场景 |
|---|---|---|
| 400 | `title 和 content 不能为空` | `title` / `content` 缺失或全空白 |

> 落库失败（DB 异常等）由全局异常处理器返回 `500`；Python 侧应对非 200 视为失败并向上返回 error，**不产生假成功**。

**curl 示例**（走网关，需带 JWT）：

```bash
curl -X POST 'http://localhost:8080/users/me/plans' \
  -H 'Authorization: Bearer <JWT>' \
  -H 'Content-Type: application/json' \
  -d '{"title":"示例方案","content":"# 方案正文 markdown"}'
```

---

### 3.2 GET /users/me/plans — 方案列表

**用途**：前端「我的方案」列表页。只查当前登录用户的方案，**不含 content 正文**，按 `created_at` 倒序；当前方案 = 列表里最新一条 `active`。

**成功响应**（`code=200`）：

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "total": 2,
    "list": [
      {
        "id": 1002,
        "title": "2026 秋招 · 后端开发三个月行动方案（v2）",
        "status": "active",
        "createdAt": "2026-09-20T10:30:00"
      },
      {
        "id": 1001,
        "title": "2026 秋招 · 后端开发三个月行动方案",
        "status": "archived",
        "createdAt": "2026-09-04T09:15:00"
      }
    ]
  }
}
```

**字段说明**：

- `createdAt` 为 `LocalDateTime.toString()` 序列化的字符串，格式 `yyyy-MM-dd'T'HH:mm:ss`（无时区）。
- 若用户尚无方案，`total=0`、`list=[]`。

---

### 3.3 GET /users/me/plans/{planId} — 方案详情

**用途**：查看某份方案全文（含 `content` markdown）。**归属校验**：只允许本人访问。

**成功响应**（`code=200`）：

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "id": 1001,
    "title": "2026 秋招 · 后端开发三个月行动方案",
    "status": "active",
    "content": "## 目标\n...（整份 markdown）...",
    "supersedesPlanId": null,
    "createdAt": "2026-09-04T09:15:00"
  }
}
```

**字段说明**：

- `supersedesPlanId`：本方案取代的旧方案 id；首版为 `null`。
- `content`：方案全文，正文详情 / 未来喂 prompt 用。

**错误响应**：

| code | msg | 场景 |
|---|---|---|
| 404 | `方案不存在或无权访问` | `planId` 不存在，或该方案不属于当前用户（越权） |

---

## 四、调用方指引

### Python 职业规划专家（plan.py）

- 只经网关 HTTP POST 落库：`settings.career_service_base_url`（默认 `http://127.0.0.1:8080`，即网关）+ `/users/me/plans`，**透传用户 JWT**（否则网关 401），写法参照 `resume_polish.save_profile_as_new`（httpx）。
- **不直连 careers 库**、不新增 plans router、不改 DB 配置。
- 生成的方案 JSON 拼成整份 `content` markdown 作为 body 发送；`sessionId` 取当前会话 id。

### 前端

- 经 Vite 代理走 `/api` → 网关 `http://localhost:8080`（`/api/users/me/plans` 等），`http.ts` 按 `{code, msg, data}` 约定解包。
- 「我的方案」页：列表 `GET /users/me/plans` → 点进 `GET /users/me/plans/{id}` 渲染 `content`（markdown）。

---

## 五、边界与约定

- `user_id` 硬隔离是安全底线：所有查询都带 `user_id` 条件，detail 未命中一律 404（不暴露存在性）。
- 方案属敏感数据，仅存方案本体（title/content/sessionId），不存其它无关隐私。
- `title` 超长（>200）入库可能报错，Python 侧生成时应先截断到合理长度。
- 同一用户可多次规划，产生多行历史快照；想拿「唯一当前方案」优先用列表里最新一条 `active`（或扩展接口时按 `status=active` 过滤）。
